# 学习闭环实施批准记录

> 批准日期：2026-08-16
> 批准人：项目负责人（当前对话明确同意）
> 状态：执行中，全部为隔离 Challenger；不改变 Champion、旧标签、原始音频或现有正式成片。

## 已批准范围

1. 建立事件级标签身份：同一输入、轨道、重叠时间段和归一化文本的历史候选不得因为边界精修或候选 ID 变化而重复要求审核。
2. 从真人反馈中归类可执行模式：至少区分 ASR/转写错误、语义保留、语义可剪、剪辑执行问题、候选误报和未知模式。
3. 将归类结果输出为带案例、反例、适用条件和状态的版本化政策卡；初期只进入 Challenger，不直接生成 human_approved 或发布权限。
4. 建立繁简分层文本：raw ASR 永久保留，match/display 文本单独归一；不修改原始词级时间戳。
5. 在隔离 Challenger 中比较 `faster-whisper small` 的 `language="zh"` 与多语自动识别配置；不覆盖已验证的 EP04 v13 结果。

## 安全边界

- 旧 `human_decisions*`、原始 WAV、现有 EDL 和正式成片只读。
- 当前 `EP04-label-loop-v1-20260815-1805` 的空白草稿不作为历史标签来源，也不继续在其上扩大审核。
- 事件级“语义决定继承”与“新边界执行质量”分开；边界变化不能伪装成旧试听已经批准。
- 任何新政策先做历史回归与独立样本验证，再由负责人审核政策卡，最后才允许影响 machine_assisted_draft。
- `autocut_policy` 继续为 `NOT_APPROVED`；不渲染、不发布、不自动提交真人决定。

## 2026-08-16 生产自动剪辑政策推进记录

项目负责人随后明确要求推进生产自动剪辑规则。已新增 `main/orchestrator/policy_promotion.py` 和
`main/runs/LABEL-LEARNING-v2-20260816/production_promotion/PROMOTION_REPORT.md`，把政策卡晋升改成
可复跑的 fail-closed 检查。

本次结论是 `NOT_APPROVED`，不是“已经启用后忘记写文档”：当前只有 2 期、1 名审核人，缺独立 benchmark、独立复核、回滚演练和明确签署的低风险删除范围。唯一识别出的 `POL-007` 是 ASR 误报的阻断候选，只能保护内容，不授权自动删剪。只有未来证据齐全、范围具体且负责人签署后，才可创建新的、可回滚的有效 `autocut_policy`；这次不改 Champion、当前审核包、EDL 或音频。

## 交付物

- 事件级身份/去重报告与契约测试；
- 反馈分类记录、政策卡和反例报告；
- 繁简匹配层与中英 ASR Challenger 报告；
- 新审核路由说明和可回滚的隔离运行证据；
- 当前状态、长期记忆、F04/F05/F08/F03 与 Agent 操作文档同步记录。
