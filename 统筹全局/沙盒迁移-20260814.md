# Codex 临时工作区迁移记录

> 日期：2026-08-14
> 目标：把后续 Agent 协作的可复用项目材料集中到桌面主项目。

## 结论

桌面目录

`/Users/renting/Desktop/minglue/剪辑项目/`

现在是唯一的协作主目录。Codex 临时工作区

`/Users/renting/Documents/ChatGPT/制作音视频剪辑工作流/`

只作为历史/迁移来源，不应继续在其中创建新的生产代码、run 或审核决定。

## 已迁移

| 来源 | 桌面项目位置 | 说明 |
| --- | --- | --- |
| `AGENTS.md` | `AGENTS.md` | 项目级阅读顺序、边界和文档规则 |
| `scripts/claude_headless.py` | `main/orchestrator/claude_headless/scripts/claude_headless.py` | Claude Code 本地无头连接器 |
| `tests/test_claude_headless.py` | `main/orchestrator/claude_headless/tests/test_claude_headless.py` | 连接器的 6 项单元测试 |

迁移前连接器测试为 `6/6 pass`；迁移后应在桌面主目录重新执行同一命令。

## 已确认桌面项目已有，不重复复制

- `deepminer-audio-editing-research/` 的研究内容已存在于 `从视频学习经验/`，除一份旧版 `source-catalog.md` 外均 SHA-256 相同；桌面版保留了更新后的路径引用。
- `deepminer-research-prompt.md` 已存在于 `端到端学习剪辑/研究资料/`，SHA-256 相同。
- `END_TO_END_CODEX.md` 已存在于 `端到端学习剪辑/说明文档/`，SHA-256 相同。
- `research/` 中的五份报告已存在于 `端到端学习剪辑/报告/`，SHA-256 相同。
- 语义分句层、EP04 V13 run、审核包和当前 Challenger 已经直接位于桌面项目，不需要从临时工作区再复制一份。

## 明确不迁移

- `.git/`、`.tools/`、Python/Swift 编译缓存和 `__pycache__`；
- `local_runs/`、`交接产物/` 中的真实音频、试听音频和大体积中间产物；
- `1R543FDOPPI0/`、`2O49K9JVF5GSA/`、`3H3G7ZME8UHBB/` 等临时模块缓存；
- 旧版双轨脚本直接覆盖桌面项目的现有版本；它们与当前 N 轨/Challenger 架构不是同一代契约。

不迁移不等于删除。来源目录保持不动，便于需要时只读追溯；新的修改统一写桌面主项目。
