# 待优化清单

> 用途：记录不阻塞当前 MVP、但以后值得做的细节。当前完成门以《当前项目进度》和《产品要求与验收标准》为准。

| 编号 | 功能 | 可优化项 | 启动条件 | 当前优先级 |
| --- | --- | --- | --- | --- |
| OPT-001 | 审核效率 | 连续审核带、候选自动定位、快捷键和批量低风险确认 | P1 基础 E2E 通过 | 高 |
| OPT-002 | 审核覆盖 | 随机抽查无候选区域，衡量漏删 | 有一次完整真人审核 | 高 |
| OPT-003 | 风险分级 | 数字、专名、否定、结论、重叠、短回应强制高风险 | P0/P1 集成后 | 高 |
| OPT-004 | ASR | 热词作为独立实验臂，不把节目答案泄漏为热词 | P0 gold 完成 | 中 |
| OPT-005 | 说话人 | speaker profile 与 CAM++ 聚类映射 | 有人工 speaker gold | 中 |
| OPT-006 | 降噪 | DeepFilterNet 短片段 A/B、延迟校准与盲听 | 工程接入和短段产物已完成；待负责人记录真人 A/B、长节目内存/分块与权重许可审计 | 中 |
| OPT-007 | 候选 | 相似案例检索和候选排序 | 多期有效审核数据 | 中 |
| OPT-008 | 统筹 | tool 注册表契约、调用器、重试和错误分级 | P0/P1 稳定后 | 高 |
| OPT-009 | 产品 | 一键创建本期、打开审核、恢复渲染和归档 | 完整 EP03 集成前 | 高 |
| OPT-010 | 监督学习 | 规则优化、GBDT 排序器、冻结 benchmark | ≥10期/≥500有效决定等门槛复核后 | 低 |
| OPT-011 | Loop | issue、人工闸门、artifact 引用、晋升和回滚 | 本地产品闭环稳定后 | 低 |
| OPT-012 | 输入类型 | 单轨、三人、采访、讲座等新路由 | 出现真实新类型素材 | 低 |
| OPT-013 | 发布规格 | LUFS、TP、码率、ducking 模板 | Mentor 拍板 | 高 |
| OPT-014 | 真实设备 | 两台独立设备 clock drift 复验 | 获得素材 | 中 |
| OPT-015 | 候选家族撤销 | cough_like / mic_bump_like 两类当前实现严重误报（`EP04-review-mixed-14-20260814-043428` 中 3/3 reject，mentor feedback 全为"没有"）；v14 从候选家族移除或阈值极高 | 已有证据；下一版规则立即执行 | 高 |
| OPT-016 | 词内匹配过滤 | filler_hesitation.strong_tokens 匹配到"额"命中 EP03 词"额度"首字，3/3 误报（"额度是一个词"）；v14 加 `next_char_blocklist` 二字词过滤字典 | 已有证据；下一版规则立即执行 | 高 |
| OPT-017 | 剪辑痕迹（rendering gate） | 本轮 4/14 feedback 明确指出剪辑痕迹明显；已从 butt-splice 升级到 50ms equal-power crossfade；mentor 复听验证未完成；备选 80-120ms 或非线性曲线 | 修复后 mentor 复听；若仍不满意继续调 | 高 |
| OPT-018 | "对/然后/这个"歧义分层 | filler_weak 列表把"对"视作纯口癖，但 mentor 反馈"对这个 X"是话题连接不是口癖；v14 拆分 backchannel vs topic_connective 子字典 | 已有证据；下一版规则立即执行 | 中 |
| OPT-019 | 句中保护(D6) | Round 2 中 N003 "不要剪（要保证完整性，这个是在句中）"；候选落在 clause-mid 时优先保留完整性；v15 已加 sentence_position_gate；EP04 用 codex semantic-transcript clause，EP03 用 gap 启发式回退 | 已有证据；v15 rule 已落 `filler-global-pause-v14/rules/candidate_rules.v15.json` | 高 |
| OPT-020 | immediate_repetition 收紧(D7) | Round 2 R-S038 "为什么要剪" + R-E306 "这个其实可以不用剪"：单纯 2-4 字重复不足以判为 stutter；v15 要求 duration_asymmetry ≥30% 或 gap<0.15s 或存在犹豫标志词 | 已有证据；v15 已落规则 | 高 |
| OPT-021 | 并轨/主麦 automix | FFmpeg 已在当前机器可用并能完成全轨渲染；剩余问题是主麦选择、串音抑制、音乐 ducking 与成片听感模板 | 固定主麦/automix 规则并完成整片盲听 | 高 |
| OPT-022 | 审核前端保存路径可见性 | 前端只在 HTTP 响应里返回 `review_draft.json` / `human_decisions.json` 的路径，页面上不显示；reviewer 提交后不知道存到哪。2026-08-15 会话里项目负责人明确反馈这个 UX 漏洞。修法：在 `审核前端/challenger-review-product-v1/mvp.html` 顶部固定显示当前 run 路径 + 草稿位置 + 提交后的正式决定位置 | 已有反馈；等主线 v22 审核完成后动 | 高 |
| OPT-023 | Preview 混音方式与最终成片一致 | 2026-08-15 EP04 v20 mentor reject C026/C028 备注"剪辑痕迹重 / 声音明显小了"。溯源发现 `build_mvp_package.py::render_ntrack_preview` 用 `amix=normalize=1`（三轨平均降 9.5 dB）+ `qsin` 曲线（中点 -6 dB）；最终成片可能走不同混音路径。契约上没写死"preview 混音必须 = 成片混音"。改法：preview 用主麦 automix（或直接取成片切片），保证 A/B 反映最终听感 | 已有证据；OPT-021 automix 落地后一并做 | 高 |
| OPT-024 | Stratum unanimous propagation 有效性 | 2026-08-15 EP04 v20 显示 orchestrator 的 stratum-unanimous 机器 propagate 机制在 v16 规则下永远空跑：v16 严格门槛使每 stratum 只 1 条候选，需要"至少 3 条且未审 sample"才能触发 unanimous propagate；结果 machine_assisted_draft ≡ human_approved bit-for-bit。V17/V18 已放宽候选生成门槛（含 cross_track_guard 0.05→0.15 s），v22 达到"12 条候选 / 6 层 / 3 层多成员 / 1 条 propagate"—— 第一次证据。要真正大规模 propagate 需要每层 5+ 条，需继续放宽或接通新家族 | 已在做（v17/v18 落地，v22 起跑）；下一步接通说错重来 / 语义重复 / 离题 / 串音归属 / 瞬态事件五家族的候选生成 | 高 |
| OPT-025 | 跨 episode 经验反馈到候选打分 | 用户核心目标："机器根据历史所有 mentor 决定的经验自动剪低风险"。当前 orchestrator 只看本 run 内 sample，不看 `mentor-feedback-regression-v1/catalog.json`。要真正做到跨 episode 学习，需要在候选生成或 machine_prediction 层加入"case retrieval + 相似度打分"—— 相似历史候选被 accept 则本次候选置信度上调、被 reject 则下调 | v22 之后立即启动；先 catalog schema 稳定，再在 orchestrator 里挂 retrieval hook | 高 |
| OPT-026 | 每次改动必须同步四处文档 | 2026-08-15 会话确认："当前项目进度.md / run 产物 / 审核前端 / 代码"必须同步。已建 `check_current_delivery_sync.py --check` 校验，但触发 FAIL 不阻断 orchestrator。需把 sync-check 加为 CI-like preflight：每次 start / resume / record-final 前自动跑一次，失败即 BLOCKED 且报告用户 | 已有基础设施；只需把 sync-check 挂入 orchestrator 主入口 preflight | 高 |
| OPT-027 | 边界精修已接入生产入口（2026-08-15 落地） | `main/orchestrator/snap_candidate_boundaries.py` 已挂进 `build_candidates_and_review`；EP04 v23b 实测 12/12 候选精修，平均边界移动 204 ms。仍需在多期节目和不同 rate/24-bit 上验证；未来加"边界精修跳过白名单"（比如强制保留 mentor 已 accept 的 exact 边界）| 已接入；仍需多期验证 | 已完成主线；跟进多期验证 |
| OPT-028 | 剪口质量预测已接入生产入口（2026-08-15 落地） | `main/orchestrator/predict_cut_artifact.py` 已挂进流程；阈值来自 65 条 mentor 历史决定中 8+ 条"剪辑痕迹" reject 的 dBFS 特征归纳。v23b 上 C042/C023 自动 BLOCK（边界 -28~-30 dBFS）、C007 判 OK（-58.9 dBFS）。未来：verdict=BLOCK 是否直接从候选池移除（当前只标注不移除）| 已接入；决定 BLOCK 是否 auto-remove | 高 |
| OPT-029 | AI 反思学习循环（不进流程）已建立契约 | 每个 AI 接手后必须：读 preferences_for_agent.md → 用户提交新 decisions 后跑 distill_preferences.py → 更新 P-XX 规则 → 会话结束前汇报改了什么。写进 Agent-SOP §9。目前只有骨架，未来加自动 sanity（比如启动时检查上次会话是否走了这个循环）| 已建契约；下一步做启动 sanity | 高 |
| OPT-030 | 未接的三个候选家族（跨轨归属 / 语义重复 / 离题） | crosstalk-candidate-v1 需要 activity classifier 前置（每个词加 primary/bleed/ambiguous）；semantic_duplicate / off_topic 需要中文 embedding 模型（400 MB）审计。先落 F04 状态清单，不阻塞主线 | activity classifier / embedding 模型审计完成后 | 中 |

新增条目时写明：问题、证据、影响、启动条件、是否阻塞当前 MVP。没有证据的想法不得直接变成 P0。
