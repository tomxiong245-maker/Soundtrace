# 标签学习驱动器 v1：执行计划

> 状态：COMPLETE（2026-08-16）
> 范围：只读历史逐项真人标签，给未来新候选生成机器建议、置信度、理由和相似案例；不写真人决定、不生成 EDL、不改音频。

## 本轮四项可验收交付

1. ✅ `label_learning_driver.py`：从有效快照提取候选特征，输出 `MACHINE_PRESERVE_SUGGESTED`、`MACHINE_CUT_SUGGESTED` 或 `HUMAN_REVIEW_REQUIRED`，并记录 case_id、反例、缺失特征、input-manifest 与来源 SHA。
2. ✅ 新增项目内 `$label-learning-driver` Skill，并把顶层、经验沉淀、Challenger、SOP、F05/F08 和交付流程同步到这一固定入口。未来新候选冻结会由 `delivery_orchestrator.py start` 自动调用；失败时 fail closed 到人工审核。
3. ✅ 防泄漏回测：按 EP03/EP04 留出，且在有数据时排除同一 source-audio identity。v4 结果为 `INSUFFICIENT_DATA_FOR_CROSS_EPISODE_GENERALIZATION`（6/65 source identity），所以不报告准确率、不缩减审核预算。**2026-08-17 起，"≥3 期节目 / ≥2 位独立审核人"数据规模 blocker 已移除**，`INSUFFICIENT_DATA_FOR_CROSS_EPISODE_GENERALIZATION` 现在只在 source_audio/bundle 覆盖不完整（防泄漏门）时出现。
4. ✅ 独立 shadow：`LABEL-LEARNING-DRIVER-v4-20260816` 只读 `EP04-label-loop-v1-20260815-1805`，12 条输出 1 建议剪 / 3 建议保留 / 8 人审；候选、overlay、审核包和草稿 SHA 前后不变。

## 明确不在本轮宣称完成的事情

- 不把机器建议伪装为 `human_accept` / `human_reject`；
- 不获得 `autocut_policy` 或自动删音频权限；
- 不承诺审核数量已降至五条以内；该结论只可由本轮回测和下一期 shadow/pilot 结果支持；
- 不修改当前冻结审核包、EDL、原始 WAV、ASR 或渲染。

## 完成判定

必须同时具备：脚本、单元测试、防泄漏回测产物、shadow prediction manifest、Skill 操作契约、当前状态文档和同步检查。任何一项缺失时状态保持 `IN_PROGRESS`。

## 最终证据与下一门

- 当前证据：`main/runs/LABEL-LEARNING-DRIVER-v4-20260816/`；v1–v3 已保留为 superseded 工程证据。
- 已验证：脚本单元测试、事件路由/政策测试、完整编排 fixture，以及冻结 EP04 的只读 shadow。
- 未通过的不是代码门，而是数据门：还缺历史案例完整的 source-audio identity（防泄漏门），才可开始测量跨期泛化或"约五条审核"的目标。**2026-08-17 起，"≥3 期独立节目 / ≥2 位独立审核人"数据规模门已按项目负责人明确指令移除。**
