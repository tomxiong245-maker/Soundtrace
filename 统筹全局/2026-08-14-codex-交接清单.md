# 2026-08-14 · Codex 交接清单 · Mac 端需要什么

> **背景**：codex 沙盒里已有产物但未同步到项目目录；Mac 本机缺 ffmpeg，走不了 Champion 流程。
> **目标**：把下列产物/工具从 codex 沙盒或外部落到项目目录，Mac 端就能重跑规划中的流程。
> **可靠度**：所有条目都是【已验证】——每条都直接对应本会话中被工程/规则/文档卡住的位置。

---

## A · 二进制工具（阻塞所有渲染）

| # | 名字 | 阻塞什么 | 期望位置 |
|---|---|---|---|
| A1 | **ffmpeg**（推荐 6.x+，静态构建） | `render_ntrack_edl.py` / `render_reference_linear_v1.py` / `delivery_orchestrator resume/record-final/recheck-qc` / loudnorm / MP3 编码 / 音乐拼接 / 任何 amix 并轨 | `.tools/ffmpeg-<ver>/ffmpeg` + 记 SHA/版本/许可到 `main/tools/tools.json` |
| A2 | **ffprobe**（通常与 ffmpeg 同发行） | QC LUFS/TP 独立复测（`recheck-qc`）、时长/采样率校验 | 同上目录 |

**审计要求**（CLAUDE.md 边界 6）：URL / 版本 / commit / 许可证 / 权重 SHA / 依赖 / 遥测 / 网络与数据流全记录，`brew install` 也要抓 formula version + prefix。

---

## B · Codex 沙盒里已做但未落项目目录

| # | 产物 | 证据 / 现状 | 期望位置 |
|---|---|---|---|
| B1 | **主麦并轨（automix / speech_mix）音频** | 用户上一轮明确说"codex 已经准备好并轨的东西了"；本机 `find` 未找到；`当前项目进度.md` 也把 automix 标为 gap | `main/runs/EP04/EP04-automix-<ts>/speech_mix.wav` + `.mp3`；`main/tools/tools.json` 登记调用脚本 |
| B2 | **ASR shootout 三引擎实跑结果** | `main/runs/ASR-SHOOTOUT-v1-20260813-2155/transcripts` 是空目录；`asr-engine-shootout-v1/README.md` 明确"沙箱资源不够，必须 mac 上做"，但 codex 可能已在别处跑 | `main/runs/ASR-SHOOTOUT-v1-<ts>/transcripts/{engine}/{track}.transcript.json` + `SHOOTOUT_COMPARISON.md` |
| B3 | **EP03 semantic-transcript**（标点） | 只有 EP04 有 `EP04-semantic-transcript-v1-20260814-120456/`；EP03 D6 目前用 gap 启发式回退，可靠度低 | `main/runs/EP03/EP03-semantic-transcript-v1-<ts>/semantic_transcripts/{male,female}.semantic.json` + manifest/ARCHIVED |
| B4 | **说错重来（self_correction）候选** | v13 `all_candidates.json` 里 `not_connected: [self_correction, ...]`；进度记 "Claude 正在单独施工" | `稳定生产/challengers/self-correction-v1/` 里的 rules + fixture 结果 + EP04 真实运行输出 |
| B5 | **语义重复（semantic_duplicate）候选** | 同上 not_connected | `稳定生产/challengers/semantic-duplicate-v1/`（需存在则同步） |
| B6 | **离题（off_topic）候选** | 同上 not_connected | 同结构 |
| B7 | **串音归属（crosstalk_attribution）** | 同上 not_connected | 同结构 |
| B8 | **瞬态事件（transient_events）v2** | 用户 round 1 反馈 cough_like/mic_bump_like 严重误报（v14 已 D1 撤销），codex 可能在做替代实现 | 同结构 |

---

## C · Champion 脚本（存在但 Mac 端跑不了）

| # | 脚本 | 缺什么 | 一旦拿到 A1，我能做什么 |
|---|---|---|---|
| C1 | `稳定生产/challengers/review-product-v1/scripts/render_ntrack_edl.py` | ffmpeg | 用 v15 EDL 同步剪三轨 + 生成 stem + speech_mix + MP3 |
| C2 | `稳定生产/challengers/intro-outro-music-v1/scripts/render_reference_linear_v1.py` | ffmpeg | reference-linear-v1 时序拼片头片尾（0-5s 纯乐 / 22s 淡入 / 37.976s 尾乐） |
| C3 | `main/orchestrator/delivery_orchestrator.py`（resume / record-final / recheck-qc） | ffmpeg + 有效 autocut_policy | 走 Champion 状态机 → 双 EDL / 双渲染 / QC / verify |
| C4 | `稳定生产/challengers/review-product-v1/scripts/render_episode.py` | ffmpeg | 单集端到端渲染入口 |
| C5 | `main/tools/tools.json` 里 18 项登记的工具 | 大多依赖 ffmpeg 或外部权重 | 走 tool-orchestrator 契约调用 |

---

## D · 数据产物（有了 A/B 后我在本机重跑就能出）

只要有 A1，我在 Mac 端可以补齐（不需要 codex）：

| # | 产物 | 依赖 |
|---|---|---|
| D1 | EP04 用 v15 规则重新生成的候选清单（应用 D1-D7） | A1 已装（读 ASR 已 OK） |
| D2 | EP03 用 v15 规则重新生成的候选清单 | A1 + B3（有 EP03 semantic 才能真正生效 D6） |
| D3 | 官方并轨（ffmpeg amix + loudnorm 两阶段）替换 round 2 预听 | A1 |
| D4 | v15 完整成片（v15 EDL + reference-linear-v1 音乐 + loudnorm） | A1（或直接让 codex 用 A1 跑） |
| D5 | v15 规则 vs mentor round 1+2 全量决定的一致率报告 | 都已在本机 |

---

## E · 排优先级（哪些先要）

**必须（做不了任何 rendering 都靠它）：**
- A1 ffmpeg
- A2 ffprobe

**强烈建议（提升 D6 EP03 可靠度）：**
- B3 EP03 semantic-transcript

**有则更好（撤销"我编造了并轨"的顾虑）：**
- B1 automix / speech_mix 产物 + 生成脚本

**可选（v15 才用到，round 2 之后才关键）：**
- B4-B8 五个 unconnected 候选家族

---

## F · 请 codex 打包的最短清单

如果只落 3 样：
1. **ffmpeg / ffprobe 静态二进制** → `.tools/ffmpeg-<ver>/`
2. **EP03 semantic-transcript run** → `main/runs/EP03/EP03-semantic-transcript-v1-<ts>/`
3. **automix / speech_mix 脚本与最近一次输出** → `稳定生产/challengers/automix-v1/` + `main/runs/EP04/EP04-automix-<ts>/`

拿到这三样，我可以：
- 用 D6+EP03 semantic 修好 EP03 那三条"靠近轨末"的模糊标注
- 用 ffmpeg 走 Champion 流程重出所有预听（loudnorm + 官方 crossfade）
- 用 v15 EDL + reference-linear-v1 + automix 出一版 EP04 完整成片给 GPT/你听最终感

---

## G · 我不需要 codex 做的（避免重复劳动）

- v15 规则 JSON（已在 `filler-global-pause-v14/rules/candidate_rules.v15.json`）
- Round 1/2 review bundle + 已收 22 条决定 + feedback（已在项目里）
- D6 clause_position 标注（round 2 review_package.json 已含）
- 偏好快照 v14/v15 delta、待优化清单 OPT-019/020/021、round 1 分析 md（已落）
