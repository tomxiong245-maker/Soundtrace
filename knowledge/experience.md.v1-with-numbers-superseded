# 剪辑经验 · 单一文件

**只有这一份文件是最新经验 SOT** · 路径: `main/knowledge/experience.md`
所有 mentor / 真人反馈 / gold cut 学到的东西都汇集在这
其他 md/json/jsonl 都是这份文件的**来源证据**,不是给 agent 读的经验

**版本**: v1 · **更新时间**: 2026-08-18T08:26:27.870614Z
**数据源**: EP03 56 gold + EP04 3 gold + EP03 熊镇正 11 决定 + 用户 chat 反馈
**分两部分**:
1. **PARAMETER** (决定怎么剪) — 参数默认值 · 工具直接读
2. **PREFERENCE** (决定剪哪些) — 剪/不剪判决 · retrieve_before_decision 读

---

## 一、PARAMETER · 决定怎么剪 (共 8 组参数)

来源: `cut_parameters.json` · workflow gold-cut-mining 从 59 条 mentor 剪辑分布反推

### 1.1 crossfade (剪口过渡)

| 场景 | 值 |
|---|---|
| 默认 | **50ms** |
| long_pause 类 | 200ms |
| EP03 风格对话 | 50ms |
| EP04 风格长停顿 | 200ms |
| 短 cut clamp | `min(target_samples, cut_duration_samples - 128)` |

**禁止用作 crossfade 调制器**: duration_ms / cut_lands_in_silence_gap / cross_track_speaking / |rms_before-rms_after|
Mentor 全期使用 constant crossfade,不因音频局部特征调整。

### 1.2 gap_before (刀前留距 · 距 prev word end 的 ms)

- **目标区间**: [120, 300] ms
- **实测中位数**: 180ms (p25=120 · p75=260)
- **硬拒绝**: gap_before < 50ms **且不在 silence gap**

### 1.3 gap_after (次词离刀后 · 距 next word start 的 ms)

- **目标区间**: [120, 450] ms
- **实测中位数**: 280ms (p25=120 · p75=440)
- **优先规则**: `gap_after ≥ 0.9 × gap_before` (尾长头短 · 28/58 gold 支持)

### 1.4 boundary_offset (落静音时深植)

- **优先 ≥**: 76ms
- **目标**: 300ms
- **上限**: 300ms (mentor 14/27 顶到 cap)

### 1.5 RMS 能量匹配

- **soft 阈值**: 15dB (软限,超了继续)
- **hard 阈值**: 25dB (人审重定位,**不加长 crossfade**)
- **middle hump ok**: 中段比两边响,只在**不在静音**时才预警

### 1.6 pause_ms_after_cut (剪后加静默)

- **默认**: 0ms
- 说明: mentor 全部 0 pause · 不再自动加静默

### 1.7 asymmetric_head_pad 按 class

| 类别 | 头 gap_before | 尾 gap_after |
|---|---|---|
| **filler_hesitation** | 210ms | 110ms |
| **pause** | 100ms | 120ms |
| **boundary_review** | 180ms | 300ms |

### 1.8 episode profile

- EP03 风格(双轨对话) · crossfade 50ms
- EP04 风格(三轨含 long_pause) · crossfade 200ms
- 未知期 · 用 50ms 兜底

---

## 二、PREFERENCE · 决定剪哪些 (共 64 条)

来源: `current.session_feedback.jsonl` · session_feedback (§20 单一 SOT)

规则按 verdict priority 排序 · never_cut > forbidden > policy > accept

### 2.1 硬负样本 · never_cut / forbidden (**永远不剪**)

- **`english_fragment`** [filler_token=go, reason_key=immediate_repetition, context=english_compound] · C014 GoGoFlow 里的 'go go' 是英文合成词内音节, ASR 拆词 artifact 不是口癖重复 · 永远不剪
- **`cross_track_backchannel`** [filler_token=['嗯', '啊', '对', '对对', '是', '是的', '好', '好的', '唉']] · 任何轨在其他轨正在说话时的短'嗯/啊/对' 都是 backchannel (跨轨附和), 无论 speaker_role · 挡候选生成
- **`asr_hallucination_english`** [filler_token=['额', '呃', '嗯'], context=english_neighbor_or_low_prob] · '额'类孤立单字若ASR附近有英文或该词 probability<0.6 → 是英文识别错误 (ASR 把 a/uh/um 误识为中文单字) · 类型化不 per-word
- **`long_pause_all_track_silence`** [kind=long_pause_in_speech] · 长停顿必须 (1) 该轨 gap ≥ 2s (2) 其他所有轨该段也全部静默 (无 ASR 词) · 之前 bug: 只查了当前轨 gap · 结果剪掉'其他嘉宾正在说话'的段 ·
- **`host_backchannel`** [filler_token=['嗯', '啊', '对', '是'], speaker_role=host] · 主持人 track_03 里的 '嗯/啊/对' 是认真听嘉宾的 backchannel · 不是口癖 · 剪了破坏对话感
- **`pronoun_no_chain_limit`** [filler_token=['什么', '怎么', '哪里', '哪个', '那个', '这个', '谁', '为什么', '什么什么', '这里', '那里'] · 用户明确 (第 3 次): 所有代词重复无论 chain 多长都不剪 · 之前 Q5 限 chain<=2 是错误理解 · '作为代词作为代词' 是自然语气 · 用户原话 'LP0
- **`historical_case_note_reject`** [reason_key=unknown] · 剪辑的时候声音明显小了
- **`pronoun_exemption`** [filler_token=['什么', '怎么', '哪里', '那个', '这个', '谁'], reason_key=immediate_repetitio] · '什么什么/怎么怎么/那个那个' 类代词/疑问词重复是自然语气加强, 去掉观感差 · 不剪
- **`never_cut_yixie`** [filler_token=一些, reason_key=immediate_repetition] · C039 一些 chain=1 不该剪 · '一些' 是内容词 (量词) 不是口癖 · 永远不进候选池
- **`mentor_final_not_cut_go`** [filler_token=go, reason_key=immediate_repetition] · 从 mentor+Sophie 双审通过的 gold EDL 反推: C014 'go' 系统剪但 mentor 最终没剪 · 说明这类候选不该 auto_cut
- **`mentor_final_not_cut_然后`** [filler_token=然后, reason_key=immediate_repetition] · 从 mentor+Sophie 双审通过的 gold EDL 反推: C023 '然后' 系统剪但 mentor 最终没剪 · 说明这类候选不该 auto_cut
- **`mentor_final_not_cut_一些`** [filler_token=一些, reason_key=immediate_repetition] · 从 mentor+Sophie 双审通过的 gold EDL 反推: C039 '一些' 系统剪但 mentor 最终没剪 · 说明这类候选不该 auto_cut
- **`mentor_final_not_cut_什麼`** [filler_token=什麼, reason_key=immediate_repetition] · 从 mentor+Sophie 双审通过的 gold EDL 反推: C036 '什麼' 系统剪但 mentor 最终没剪 · 说明这类候选不该 auto_cut
- **`emphasis_repetition_never_cut`** [reason_key=immediate_repetition, text_pattern=(特别|非常|真的|很|太|超)+] · 修辞性强调重复 (特别特别/非常非常) 不能剪 · EP03 C021 C022 reject 证据
  - 证据: C021, C022
- **`self_write`** [any=True] · agent 不能自由发挥自己写候选生成/boundary/gate/mix/clip 脚本 · 做过的都是工具, 找不到查 tools.json 再查 skills
- **`always_call_feedback_first_retrieval_before_decide`** [any=True] · 我 v215/v216 直接跑逻辑不查反馈 · 用户明确 CLAUDE.md §18 加了 skill · **必须** 每决策前先调 retrieve_before_decisi
- **`never_eat_neighbor_word`** [any=True] · 用户明确 (2026-08-18 16:00): 'c007 的额的前一个词剪辑掉了 · c034 更夸张剪掉了两个词 · 不能剪辑相关的词'. edge_extend 或 cro

### 2.2 需要扩展 · needs_extension (chain 场景保留最后 1 个)

- **`boundary`** · 边界扩 100ms 不够 · 呃字呼吸头尾都要盖到 · 至少 150-200ms
- **`filler_boundary_edge_extend_150_200ms`** · 根因 (2 次重复): filler 单字 (呃/嗯/啊/额) 剪时必须扩 150-200ms · 之前 20ms 不够 · v217 用 180ms

### 2.3 主战场 · promote / prefer (**应该剪**)

- **`gold_synth_semantic_boundary_primary_target`** · [promote] · LENS5 SEM_P1 主体类（42/59=71.2%，中位 2020ms）+ LENS1 P1/P2/P3/P4（p25-p75 gap 主区间，boundary_review 65% 命中 si
- **`gold_synth_filler_boundary_lock_v2`** · [promote] · LENS5 SEM_P2（6/6 短 cut = 5 immediate_rep + 1 long_pause，pure_filler=0）+ 用户 memory filler_boundary_lo
- **`gold_synth_reject_pure_filler_and_rhetorical`** · [promote] · LENS5 SEM_P4 强负样本（59/59 缺席）。'剪光所有语气词' 与 Mentor 行为完全相反，是硬约束。
- **`gold_synth_self_correction_all_or_none`** · [promote] · LENS5 SEM_P3 均值 5175ms（n=2）+ 用户 memory self_correction 全或无。样本量偏小，medium confidence。
- **`gold_synth_immediate_rep_in_speech_allowed`** · [promote] · LENS5 SEM_P6（immediate_rep 11/12 未落静默）+ LENS1 pause 分类 13/13 lands=False。打破 '刀必落静默' 前提。
- **`gold_synth_crossfade_policy_episode_constant_with_category_override`** · [propose] · 融合 LENS3 P1/P6 + LENS5 SEM_P5 + LENS4 HOW-RMS-03。50ms 是稳定基线，仅 long_pause / EP04 三轨 profile 用 200ms；短
- **`gold_synth_boundary_offset_zone_and_tie_breakers`** · [propose] · LENS1 P1+P3+P4 + candidate_gate_gap_window + candidate_reject_glued_to_word + candidate_prefer_tail_
- **`gold_synth_boundary_rms_tolerance_and_regime_accept`** · [propose] · LENS4 HOW-RMS-01/02/03/04/05 合并。48% gold >10dB，14% >25dB；asymmetric-silence 38% 最大。要求 silence-silenc
- **`gold_synth_cross_track_speaking_redefine_and_host_silent_gate`** · [propose] · LENS2 P1+P2+P3 + LENS3 P5 合并。当前 cross_track_speaking 100% True 是同人双麦 bleed 伪信号；重定义后可释放真 overlap 信号。E
- **`ep03_gold_confirms_P-04_long_pause`** · [pattern_confirmed] · EP03 mentor 剪了 25 条命中 P-04_long_pause · 案例 ['C001', 'C003', 'C004', 'C005', 'C006']
- **`ep03_gold_confirms_P-07_mentor_boundary_reverse`** · [pattern_confirmed] · EP03 mentor 剪了 15 条命中 P-07_mentor_boundary_reverse · 案例 ['C002', 'C009', 'C018', 'C020', 'C022']
- **`ep03_gold_confirms_P-05_immediate_rep`** · [pattern_confirmed] · EP03 mentor 剪了 1 条命中 P-05_immediate_rep · 案例 ['C014']

### 2.4 需要人审 · human_review_required

- **`ep03_filler_hesitation_split`** · EP03 filler_hesitation 2 accept 2 reject · 时长无法区分, 必走人审
- **`content_word_repetition_needs_context_check`** · 实词/双字词自身重复 (人类人类/设计设计) 常是口语强调, 不是说错重来 · EP03 C013 C018 C006 reject

### 2.5 其他 policy / accept / bias_reject

- **`tools_installed_must_use`** · [policy] · 用户明确 (2026-08-18 早上): '能用工具就用工具, 我们都下完了为什么不用'. 装了的开源包 (pydub / librosa / noisereduce / ffmpeg filter
- **`splice_must_be_pydub_or_ffmpeg_crossfade`** · [policy] · 剪口拼接必须用 pydub AudioSegment.crossfade 或 ffmpeg acrossfade + room tone splice. 禁止 concat + afade + anu
- **`crossfade_preserves_consonant_onset`** · [policy] · YouTube § 2 (Clean Cut Audio): 不在辅音刚开始位置硬切, 要**保留一部分辅音起音放进 crossfade**. 单纯删除 filler 会把相邻词挤得不自然. 中文的爆
- **`crossfade_length_range`** · [policy] · YouTube § 2: crossfade 守护范围 20-80ms, 50ms 默认. 中文长辅音可能需要更长. 每处独立选择, 不强制固定 50ms.
- **`room_tone_from_same_recording`** · [policy] · YouTube § 5 (Randy Rektor): room tone 定义为实际空间的噪声底. 硬删除和淡化后仍可能听到'空间消失'. 必须从**同一原始轨、同一会话、同一录制条件**提取干净静
- **`mp3_peak_verify_after_encode`** · [policy] · YouTube § 3 (Incidence Studio): MP3 编码后重建峰值可能高于源 WAV. 必须对完整 MP3 解码后重新记录 LUFS/LRA/TP/时长/采样率/声道/码率, 不能
- **`ripple_delete_all_tracks_sync`** · [policy] · YouTube § 4 (REAPER Blog): 改变节目时长的删除必须**同步作用于所有轨**(global_ripple_delete). 单轨 gain/mute/bleed 只能是 tra
- **`sync_check_start_and_drift_separately`** · [policy] · YouTube § 1 (Curtis Judd): 时间码只能对齐起点. 长录音末尾仍可能漂移. 同步检查必须分开: (1) offset (起点偏移) (2) clock drift (漂移). 
- **`preview_and_final_must_match_mix`** · [policy] · Preflight OPT-023: preview 混音必须与最终成片同一路径, 否则用户听 preview 判断错. 目前 preview 用 amix normalize=1 (三轨平均 -9.
- **`external_tool_audit_required`** · [policy] · CLAUDE.md 边界 §6 + Preflight § 1: 装 ffmpeg/MFA/pyannote 等外部工具必须在 tools.json 登记版本 + SHA + 许可证 + 依赖 + 遥
- **`mentor_final_gate`** · [policy] · mentor 反馈 (2026-08-17): 内容审 = mentor 整片听审; 音量审 = 项目负责人 loudnorm 验收. 双审通过才能升级 human_approved, 缺一都是 ma
- **`distiller_after_review`** · [policy] · Preflight: agent 会话结束前必须 (1) 读 preferences_for_agent.md (2) 用户新审核后跑 editing_experience_distiller. 否则
- **`pause_dynamic_by_cut_count`** · [policy] · 用户明确 (2026-08-18 13:00): '剪辑一个的留白和剪辑两个的留白不能一样'. 动态 pause = cut_count × 60ms + segment_separator_bonu
- **`chain_cut_dont_eat_kept_word`** · [policy] · v212 bug: 剪前 3 个然后 [959.34, 960.10] 但保留词 start=960.080 · edge_extend 40ms 把保留词吃掉. 修法: cut_end 若 + ed
- **`cut_boundary_from_librosa_onset`** · [policy] · 用户 '然后被吞'. 根因: ASR 报的 word start 比真实辅音起音晚 14.7ms · v213 v214 cut_end 用 ASR 数字都吃到辅音. 修法: librosa.onse
- **`cut_boundary_must_be_asr_word_not_edl`** · [policy] · 根因: EDL render_sync_cut 边界是 v20 filler_boundary_lock 缩到稳定内段 (386ms) · 不是 ASR 整词 (680ms). '呃' 呼吸头尾 15
- **`single_version_current`** · [policy] · 用户明确 (2026-08-18 16:00): '今后只留一个版本 · 每次更新'. 每次生成 A/B clip 只保留最新 · 覆盖前一版 · 不再 v215/v216/v217 平行目录.
- **`solo_speaker_immediate_repetition_bias_reject`** · [bias_reject] · 独自讲话 (无 cross_track_speaking) 时的 immediate_repetition · EP03 5/5 全 reject · 视为修辞/强调, 不剪
- **`ep03_gold_new_pattern_semantic_boundary_review_mentor`** · [cut_precedent] · EP03 mentor 剪了 15 处新模式 · semantic_boundary_review_mentor · 需要 Challenger 假设

---

## 三、跨 lens 5 条 top findings (agent 优先读)

1. Mentor 剪的是 semantic boundary 与 immediate repetition，不是孤立 filler：LENS5 显示 semantic_boundary 71.2% + immediate_rep 20.3%，pure_filler=0 / rhetorical_emphasis=0；LENS1 boundary_review 65% 命中 silence gap 且 offset 中位 300ms（14/27 顶到 300ms 上限）；LENS4 半数（29/58）中段 RMS 明显高于两边（hump），说明剪掉的是有声内容的语义整段，而非静默里的语气词。三个 lens 互相印证 '找边界' 是主战场、'砍 filler' 是次要辅助。

2. mentor_crossfade_ms 不是局部特征函数，而是按 episode/类别 constant：LENS3 EP03 56/56=50ms，EP04 3/3≈200ms（其中 192.583ms 是 385ms 短 cut 的 window-clamp 量化产物）；LENS4 |RMS diff|<=3dB / 3-10 / >10 三桶中位数全部 50ms，最大 33.9dB mismatch 也不加长 fade；LENS5 只有 long_pause 类均值 197.5ms，其余三类严格 50ms。跨 lens 一致否定 duration/gold_category(局部)/silence_gap/cross_track/RMS-diff 作为 xfade 调制器。

3. cross_track_speaking 目前的定义已失效：LENS2 EP03 56/56=True 且两轨 covered_words 全非空、EP04 C034/C044 同文本 '就是我们*'/'因为' 完全是同一嘉宾双麦 bleed；LENS3 因该 flag 恒真而无法作为 xfade 调制信号。需按 (speaker_map[a]!=speaker_map[b]) AND (normalized(text_a)!=normalized(text_b)) 重新定义，否则 host+guest 真重叠信号被彻底掩盖。

4. 下刀点分布高度结构化：mid-pause + 深植静音 + 尾长头短：LENS1 gap_before p25=120 / median=180 / p75=260ms，gap_after p25=120 / median=280 / p75=440ms（尾长 28 / 头长 20 / 对称 10），命中 silence gap 时 boundary_offset 中位 300ms 且 14/27 顶到 300ms 上限；LENS2 EP04 3/3 需 host 静默且 host gap_before≥1901ms / gap_after≥2604ms；紧贴词界（gb<50ms 且不在 silence gap）仅 3/58 且全属 pause 类。

5. 边界 RMS 不匹配是被容忍的副产物，不是判定信号：LENS4 |before-after| >10dB 占 48%、>25dB 占 14%、极值 33.9dB；三种 regime（silence-silence 19% / asymmetric-silence 38% 最大 / speech-speech 28%）都出现在 gold 中；LENS3 crossfade 不为 RMS mismatch 加长；LENS5 immediate_rep 类 11/12 落在活跃语音中。要求 'silence-silence 边界' 会拒绝多数 mentor-approved 剪辑。

---

## 四、系统缺口 (需要写代码 · 不是加规则)

1. generate_comprehensive_cut.py 若按 'silence-silence 边界 + 短 filler token 直接砍' 直觉出候选，会与 Mentor 行为直接对立：LENS5 pure_filler=0 / rhetorical=0，LENS4 asymmetric-silence 38% 是最大 regime，semantic_boundary 71.2% 覆盖 600ms+ 语义整段——需要新增 'semantic_boundary_primary_target' 通道与 filler 负样本硬约束。
2. 缺少按语义类别 route 的 crossfade 策略：现脚本大概率单一默认 xfade。需要按 profile+class 双层查表（EP03=50 / EP04=200 / long_pause 类覆写 200 / 其他 50）并在短 cut 上做 window clamp（min(target, dur - 128 samples)）。
3. cross_track_speaking 判定错误：当前 flag 只看 '两轨都有文本'，把同人双麦 bleed 与真 host+guest overlap 混为一谈（EP03 100% true, EP04 C034/C044 同文本 bleed）。需引入 speaker_map + normalized(text) 差异判定，否则 host-silent gate、solo-guest router 都无法建立。
4. 缺少 host-silent gate / host-protect：EP04 3/3 gold 需 host.covered_word_texts=='' 且 host gap>=1.5s；脚本若允许 main_track==host 会产生非 gold 候选。soft veto 需要 host_track_id 与 speaker_map 元数据支持。
5. 缺少 boundary-offset 精细 zone：当前脚本可能仅 '找 silence gap 中心'，但 gold 中位深植 300ms、14/27 顶到 300ms 上限、p25=76ms 是软下限；同时需要 gb<50ms 且不在 silence 的硬拒绝分支与 tail-heavier tie-breaker（ga>=0.9*gb）。
6. 缺少 immediate_rep in-speech 通道：当前脚本若强制 'cut 必须落在 silence gap'，会拒掉 11/12 mentor immediate_rep 短 cut。需要独立 in-speech 剪路径 + 50ms xfade 抹平。
7. 缺少 self_correction 全或无判定：需要自纠正 marker 词表 + 双轨语义漂移（overlap<3 chars）检测 + duration>=4000ms 联合触发，且必须整段清除而非局部。
8. 缺少 boundary RMS 阈值配置：soft 15dB / hard 25dB；middle-hump 只在 rms_middle-max(edges)>10dB 且不在 silence 时才报警——脚本若使用 '中段更响=可疑' 的默认启发式会误报 50% gold。
9. 缺少 episode profile 概念：xfade 是 per-episode/production constant 而非 per-cut。脚本需读取 profile.json 或从近邻已批准剧集回填 xfade 默认，不要基于本地音频特征回归。
10. 分类启发式不足：LENS5 semantic_boundary 是兜底类，可能混入本可拆分的 immediate_rep/self_correction；脚本内部分类器需与 raw gold_category（boundary_review/pause/filler_hesitation）+ 字符重复检测 + self_correction marker 三路并行，避免误归。


---

## 五、待验证假设 (需要 Challenger)

1. 拉取 >=3 集额外 gold 数据（尤其含 intra-episode xfade 变化的集），验证 xfade 是 per-episode constant 还是 per-project constant——目前 EP03/EP04 各自恒定，无法区分。
2. 构造 cross_track_speaking=false 的样本切片（单轨或明确单人段），验证重定义后的 flag 是否恢复信号；并检测重定义后 EP04 C034/C044 是否翻转为 False。
3. 扩大 self_correction 样本至 n>=10（当前仅 2 例 GOLD_C027/C044+EP04_C034），验证 '全或无 & duration>=4000ms' 规则；引入自纠正 marker 词表在 EP01/EP02 反向标注。
4. 在 EP04 之外寻找 host+guest 真重叠（非双麦 bleed）的 gold cut，确认 host-silent gate 是硬规则还是 soft prior，并测量 host 允许的最小 gap 阈值。
5. 跑一次 challenger：用当前 generate_comprehensive_cut.py 生成候选 vs Mentor gold 做混淆矩阵，特别检查 pure_filler/rhetorical 假阳率与 semantic_boundary 假阴率，量化 '砍 filler 直觉' 造成的偏移。
6. 对 42 条 semantic_boundary 做二次拆分：用主题分割 / 说话人转换 / 时间戳文本相似度回归，判断兜底类内部是否隐藏可拆分的 immediate_rep/self_correction，反查 lens5 分类器的 recall 上限。
7. 在紧贴词界 gb<50ms 的 3 条样本（GOLD_C008/C021/C029）做逐一听审，确认硬拒绝规则是否会误伤真实的 pause-edge 场景。
8. 为 boundary_offset 300ms 顶格（14/27）做因果分析：是 Mentor 主观深植还是工具默认 padding？读取工程文件里的 nudge/offset 参数验证。
9. 验证 EP04 三条 200ms xfade 是否与 '三轨多人过渡' 强绑定，还是仅与 long_pause 类相关；构造只有 long_pause 但双轨的样本切片测试。
10. 为 RMS 15dB soft / 25dB hard 阈值做 ROC 分析：在非 gold 候选（generate_comprehensive_cut.py 产出的 rejected 候选）上跑，看阈值是否能区分 gold vs non-gold，或是完全独立于 gold 判定。


---

## 六、Agent 使用说明

### 决策前必调 (CLAUDE.md §18)

```python
from feedback_engine import retrieve_before_decision, load_cut_parameters

# PREFERENCE · 决定剪不剪
prefs = retrieve_before_decision(candidate, 'cut_boundary', 'EP04',
                                  knowledge_category='PREFERENCE')
if prefs and prefs[0]['verdict'] in ('never_cut','forbidden'):
    return SKIP

# PARAMETER · 决定怎么剪
params = load_cut_parameters()
crossfade = params['how_to_cut_defaults']['crossfade_ms']['default']
```

### 决策后新反馈 (三学习流路由 · 见 `docs/learning-flow-selector.md`)

| 数据 shape | 用哪条流 | 落地 |
|---|---|---|
| chat 单条反馈 | feedback-engine analyze | 更新本文件 (§2) |
| 一批 accept/reject | label-learning-driver | 更新本文件 (§2 + §1) |
| Mentor gold EDL | editing-experience-distiller | 更新本文件 (§1 + §2) |

**每次学到新经验都 append 到 `current.session_feedback.jsonl` (v2 schema 带 knowledge_category) 或 `cut_parameters.json`,然后重新生成本文件。**

---

## 七、生成本文件

```bash
python3 -c "
# 从 session_feedback.jsonl + cut_parameters.json + synthesis.json 汇总
# 见 experience.md 生成脚本
"
```

**本文件是唯一 agent 读的经验 SOT · 其他 md/json/jsonl 是来源证据。**


---

## 八、事故记录 · 2026-08-18-1750 · 剪不干净根因

**用户反馈**: "剪不干净 · 工具明确能够剪辑干净, 你为什么还不用呢"

**根因链**:

1. **§11 违反**: 我自行写了 `generate_comprehensive_cut.py` (v219) 替代唯一合规入口 `generate_ab_clip_learning_driven.py` (v215) · 已 rename 为 `.deprecated_v219_violated_11`

2. **narrow-cut 过度触发**: v219 消费 session_feedback 里 `c034_cut_too_much` 规则 (verdict=cut_scope_too_wide) 收窄到 200ms · 但 §19 相邻词保护后**这条规则已过时** · 现已标 `superseded`

3. **candidate range vs ASR word range 不匹配**:
   - candidate.start_seconds/end_seconds 来自 auto-detected 部分 · 通常是 word core (~385ms)
   - ASR word range 完整 word (~680ms)
   - 用 candidate range 剪 → 前后各 ~150ms word 残留
   - **正确做法**: 用 ASR word `[start_seconds, end_seconds]` · 左右各留 20ms 相邻词保护 (§19)

4. **feedback_engine 两个 bug 顺带修**:
   - `retrieve_before_decision` 二次 timestamp sort 覆盖 verdict priority · 已修
   - `is_never_cut` 匹配太宽 · 仅按 reason_key 命中 · 已改严格 (filler_token/text_pattern + context)

**规则**:
- 剪辑候选 · cut_start = `ASR_word.start_s + 0.020` · cut_end = `ASR_word.end_s - 0.020`
- chain 场景 · cut_end = `min(ASR_word.end_s - 0.020, kept_word.start_s - 0.020)`
- **禁止**用 candidate.start_seconds/end_seconds 作 cut range (那是 core 部分, 剪不干净)
- **禁止**agent 自写 A/B clip generator · 只用 `generate_ab_clip_learning_driven.py` (v215)

案例 case_id: C007_呃 (candidate 385ms vs ASR 680ms) · C034_我们 (v219 narrow 200ms vs v215 517ms 全 word)

---

## 九、candidate 已自带精确参数 · 别乱改

**发现时间**: 2026-08-18-1800 · 用户 "你都没改我才 · 剪不干净"

每个 candidate 里已有精确参数,agent **禁止**用 ASR word range 或其他方法覆盖:

- `candidate.start_seconds` / `end_seconds` — 已 zero-crossing snap 到 ±150ms 窗口内静音区(见 `boundary_snap.start_moved_samples` / `end_moved_samples`)
- `candidate.boundary_lock: True` — 声明"此边界已锁 · 不许改"
- `candidate.post_cut_pause_ms` — 精确 pause 长度 (C007=40ms · C034=350ms · 各不相同)
- `candidate.artifact_risk.verdict` + `boundary_dbfs` — 剪口质量预告 (OK/HUMAN_REVIEW/BLOCK)

**正确用法**:
```bash
python generate_ab_clip_learning_driven.py \
  --cut-start-seconds <candidate.start_seconds> \
  --cut-end-seconds   <candidate.end_seconds> \
  --pause-ms-override <candidate.post_cut_pause_ms> \  # v215.1 加 · 尊重 boundary_lock
  --kept-word-asr-start <chain[last].start_seconds if chain>
```

**错误示例** (今晚 3 次 iterate 都犯):
- 用 ASR word 全范围 (呃 680ms) 剪 → 前后 300ms 是 就是 / 它 的邻域,剪到邻词
- 用 ASR word ±20ms guard → 违反 candidate boundary_lock (candidate 已 snap 到静音区,再 guard 双重收窄)
- 用 `dynamic_pause_ms(n_cut)` 忽略 candidate.post_cut_pause_ms · 40ms vs 80ms 差别很大

**违反 boundary_lock 视为破坏契约** · 已加 v215.1 `--pause-ms-override` 支持 candidate 参数直传。
