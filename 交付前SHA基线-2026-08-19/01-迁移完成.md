# 最终交付文档 · 架构瘦身迁移完成 · 2026-08-19

> **触发**：Session 1 + 1.5 的架构改动原本改在活代码 (`/剪辑项目/`)，用户 20:20 后指出方向错了 · 应该改在 `交付/最终交付文档/`。本文档记录把整套改动**迁移**到最终交付文档的过程。

## 迁移前后对比

| 项 | 迁移前 | 迁移后 |
|---|---|---|
| CLAUDE.md 行数 | 114 | **82** (-28%) |
| CLAUDE.md 具体条款 body | 22 条并列 | **0**（全搬到 domain-rules.md） |
| tools.json 项数 | 57 | **55** |
| v2 registry adapter 数 | 20 | **54** |
| pipeline critical adapter 覆盖 | 1/20 | **19/19** |
| cut-verify 6 项 adapter | 0/6 | **6/6** |
| §11 未登记即报错（verify.sh Layer 21） | 缺 | **有 · FAIL** |
| §15 装了必须用（verify.sh Layer 18） | 缺 | **FAIL-closed** |
| delivery_orchestrator v2 桥 | 无 | **有 · --executor v2 opt-in** |
| speaker_role_gate 缺 map 处理 | 静默 skip | **SystemExit fail-closed** |
| LEGACY SHIM (`feedback_first_retrieval` · `user_feedback_analyzer`) | 存在 | **已删除** |

## 迁移动作三批

### 批次 1 · 新增 8 + 删除 2（零风险）

**新增**：
- `verify/scripts/verify/layer_21_tool_registration.py`
- `代码/main/tools/tool_registration_allowlist.json`
- `项目主文档/domain-rules.md`
- `新skill/candidate-family-integration/SKILL.md` (deprecated stub)
- `新skill/editing-experience-distiller/SKILL.md` (deprecated stub)
- `新skill/integration-governance/SKILL.md` (deprecated stub)
- `新skill/label-learning-driver/SKILL.md` (deprecated stub)
- `新skill/podcast-editing-orchestrator/SKILL.md` (deprecated stub)

**删除**：
- `代码/main/orchestrator/feedback_first_retrieval.py`（LEGACY SHIM · 已并入 feedback_engine）
- `代码/main/orchestrator/user_feedback_analyzer.py`（同上）

### 批次 2 · 覆盖 12 个 differs

- `项目主文档/CLAUDE.md`（6 元规则版）
- `verify/verify.sh`（Layer 21 + Layer 18 升 FAIL）
- `verify/scripts/verify/layer_18_installed_vs_used.py`
- `代码/main/tools/tools.json`（55 项 · v2_status 标签齐全）
- `代码/main/tools/audits/BACKLOG.md`
- `代码/main/orchestrator/delivery_orchestrator.py`（含 v2 桥 + --executor CLI flag）
- `代码/稳定生产/challengers/e2e-auto-runner-v1/scripts/run_end_to_end.py`（speaker_map fail-closed）
- `代码/稳定生产/challengers/tool-orchestrator-v2/adapters/registry.json`
- `代码/稳定生产/challengers/tool-orchestrator-v2/adapters/_adapter_base.py`（+ skeleton 豁免）
- `代码/稳定生产/challengers/tool-orchestrator-v2/adapters/generic_script_adapter.py`（+ `{name*}` 展开）
- `代码/稳定生产/challengers/tool-orchestrator-v2/contracts/adapter.schema.v1.json`（+ skeleton 字段）
- `代码/稳定生产/challengers/tool-orchestrator-v2/tests/test_registry_contracts.py`

### 批次 3 · 交付布局专属修正

live 布局与交付布局有目录差异，需要交付专属路径修正：

1. **tools.json cut-verify 6 项 full_path**：`skills/cut-verify/` → `新skill/cut-verify/`
2. **registry.json 53 项 wraps_script 前缀重写**：
   - `main/` → `代码/main/`
   - `稳定生产/` → `代码/稳定生产/`
   - `端到端学习剪辑/` → `代码/端到端学习剪辑/`
   - `skills/cut-verify/` → `新skill/cut-verify/`
   - `benchmark/` · `verify/` 保持不变（在交付根）
3. **verify layer_18 · layer_21 脚本 PROJECT_ROOT 探测自适应**：改为"往上找 CLAUDE.md"逻辑 · 兼容 live 与交付两种布局
4. **test_registry_contracts.py PROJECT_ROOT 自适应** · 加 `_CODE_PREFIX` 判定

## 验证结果

```
verify.sh · 全绿
  第 12 层 · tools.json full_path 可达 · 55/55 PASS
  第 13 层 · skills SKILL.md 结构 · active=8 deprecated=5 index=1 · PASS
  第 18 层 · installed vs used · WARN 但不 block（transitive 依赖常见）· PASS
  第 21 层 · main/orchestrator/*.py 全部登记或在白名单内 · PASS

v2 契约测试 · 22/22 · OK
```

## 严格保留的边界（未动）

- `新skill/cut-verify/**`（脚本内容）· `knowledge/cut_parameters.json` · `knowledge/session_feedback/**` · A/B clip 相关脚本
- 8 个 active skill：audition-and-delivery · candidate-generation-and-gate · cut-verify · episode-triage-and-plan · experience-driven-review · feedback-engine · governance-and-tool-registry · learning-and-experience
- 其它顶层目录：`benchmark/` · `docs/` · `knowledge/` · `参考成品/` · `审计报告/` · `与AI的上下文/` · `统筹全局/` · `新落地/` · `新产出/` · `deprecated备份/`
- 代码目录：`代码/端到端学习剪辑/` · `代码/main/orchestrator/orchestrator.py` · `代码/main/orchestrator/feedback_engine.py` · `代码/main/orchestrator/generate_ab_clip_learning_driven.py` · `代码/main/orchestrator/make_edl_ab_clips.py` · `代码/main/tools/tool_lookup.py`

## 快速使用

```bash
cd 交付/最终交付文档/

# 静态校验
bash verify/verify.sh

# v2 契约测试
cd 代码/稳定生产/challengers/tool-orchestrator-v2 && python3 -m unittest discover -s tests -p 'test_*.py'

# 用 v2 executor 跑（EP0X）
python3 代码/main/orchestrator/delivery_orchestrator.py --executor v2 start --input-dir ... --episode-id EP0X
```

## 验证依据（三档语气）

**已验证事实**：
- verify.sh 4 层全绿 · v2 22 项契约测试 OK
- tools.json 55 项 · v2 registry 54 adapter · pipeline critical 19/19 覆盖 · cut-verify 6/6 覆盖
- CLAUDE.md 82 行 · docs/domain-rules.md 133 行 · §1-§22 anchor 完整
- LEGACY SHIM 2 项删除 · deprecated skill 5 项 stub 化
- 交付布局与 live 布局路径差异全部消化

**已决定的方向**：交付版即 mentor / agent 拿到就能直接用的完整版本 · 从 0 开始 self-contained。

**待验证假设**：
- v2 executor opt-in 路径的 EP04 端到端等价性 · 用户手动跑 · 不由 agent 自动跑
- deprecated skill stub 是否会让某些 legacy caller 崩掉 · 需要真跑 pipeline 验证

## 与活代码的关系

活代码 `/剪辑项目/` 里也有整套同样架构（这是我最初改错的地方）。**两处一致**（除交付布局路径修正外）。未来如需 sync 只需：

- live → 交付：复制文件 + 应用批次 3 布局路径修正
- 交付 → live：复制文件 + 反向路径修正（`代码/` 剥离 · `新skill/` → `skills/`）
